# 仅供本仓库开发安装使用（不在 package.json 的 files 白名单里，不会随包分发）。
#
# 为什么必须 legacy-peer-deps：DSH 宿主组合是几十个互为 peerDependency 的
# 预发布包（0.1.5-rc.2），而 npm 对 `^0.1.5-rc.2` 这类预发布范围的解析很挑剔，
# 严格模式下几乎必然 ERESOLVE。这些 peer 本来就由宿主的 profile 提供，
# devDependencies 里逐个列出来就是为了在本仓库跑 host 侧类型检查与测试。
legacy-peer-deps=true
